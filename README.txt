Chris Wilhoite

1. I concatenated the "$" to my "totalCostReturn" variable that stored the result from my "calcTotalCost()" function that retured the total cost for the pizzas. 
2. To initially hide the views and make them visible again, I used the android:visibility feature to make all of the elements in which I wanteded invisible to 
remain hidden until the user clicks on the "CALCULATE!" button. 